# 仅用于「浏览器开发模式代理」(npm run server) 与「CLI 批量」(node cli/batch.js --ai / --search)
# 桌面安装版不读取此文件：密钥在应用「设置」页填写，保存在本机用户数据目录。

# AI（OpenAI-compatible）
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com
OPENAI_MODEL=gpt-4o

# 搜索增强（二选一）
SERPER_API_KEY=
TAVILY_API_KEY=
