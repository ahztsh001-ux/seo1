name: Build Windows EXE

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts   # 优先使用 lock，如果没有就自动生成

      - name: Build & Package EXE
        run: npm run build:app

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: SEO-Pipeline-Windows-安装包
          path: |
            release/*.exe
            release/*.blockmap
